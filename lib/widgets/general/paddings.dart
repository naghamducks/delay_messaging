import 'package:flutter/material.dart';

const listViewPadding = EdgeInsets.all(20.0);
const buttonIconPadding = EdgeInsets.all(5.0);
const listViewHeaderPadding = EdgeInsets.symmetric(horizontal: 2.5);
const gridViewPadding = EdgeInsets.only(top: 16, bottom: 16);
const smallHighlightedTextPadding = EdgeInsets.symmetric(horizontal: 10, vertical: 5);
const highlightedTextPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 10);
const headerIconPadding = EdgeInsets.all(7.0);
const detailedCardItemPadding = EdgeInsets.only(bottom: 10);
const detailedCardPadding = EdgeInsets.only(left: 24, right: 24, top: 24, bottom: 10);
const dropdownInputPadding = EdgeInsets.symmetric(vertical: 20.0, horizontal: 20.0);
const dropdownItemsPadding = EdgeInsets.symmetric(vertical: 10.0, horizontal: 25.0);
const cardPadding = EdgeInsets.only(top: 20, bottom: 0, left: 20, right: 20);
const buttonPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 9);
const popupPadding = EdgeInsets.fromLTRB(20, 0, 20, 20);
const popupTitlePadding = EdgeInsets.only(top: 20, bottom: 0, left: 20, right: 20);
const resultPopupPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 10);
const resultButtonsPopupPadding = EdgeInsets.symmetric(horizontal: 75, vertical: 30);
const labelPadding = EdgeInsets.fromLTRB(10, 2, 10, 2);
const innerInputPadding = EdgeInsets.only(left: 10.0, top: 2.0, bottom: 2,right: 0);
const innerInputMaxLinesPadding = EdgeInsets.fromLTRB(10, 15, 10, 15);
const accordionHeaderPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 10);
const accordionContentPadding = EdgeInsets.symmetric(horizontal: 0, vertical: 20);
const sideNavListPadding = EdgeInsets.fromLTRB(12, 8, 0, 8);
const sideNavHeaderPadding =  EdgeInsets.symmetric(horizontal: 20,vertical: 13);
const sideNavVersionPadding =  EdgeInsets.all( 20);
const messageErrorPadding =   EdgeInsets.symmetric(horizontal: 20, vertical: 10);
const appBarButtonIconPadding = EdgeInsets.all(3.0);
const iconPadding = EdgeInsets.all(10.0);
const largePadding= EdgeInsets.all(20.0);
const smallPadding= EdgeInsets.all(10.0);